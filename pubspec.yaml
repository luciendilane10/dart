name: task_manager
description: Application de gestion de tâches CLI en Dart.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Dépendances principales de l'application (si nécessaires)

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0